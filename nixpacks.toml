[phases.install]
cmds = ["pnpm install --no-frozen-lockfile"]

[phases.build]
cmds = ["pnpm --filter @workspace/fanta11 build && pnpm --filter @workspace/api-server build"]

[start]
cmd = "cd artifacts/api-server && node --enable-source-maps ./dist/index.mjs"
