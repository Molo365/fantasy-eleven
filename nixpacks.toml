[phases.install]
cmds = ["pnpm install --no-frozen-lockfile"]

[phases.build]
cmds = ["pnpm run -C artifacts/fanta11 build && pnpm run -C artifacts/api-server build"]

[start]
cmd = "node --enable-source-maps /app/artifacts/api-server/dist/index.mjs"
