Redesign the Dashboard page to be competition-aware and show both Premier League and Serie A at a glance, per the attached reference mock (thin two-league strip, no big hero section).

CONTEXT (confirmed by prior investigation, do not re-investigate):
- artifacts/fanta11/src/pages/dashboard.tsx currently derives the active team from
  authState.user.teamId (legacy single-team assumption) and ignores
  useLeagueContext()'s activeTeamId/activeCompetitionKey, and user.teams[].
- GET /api/dashboard/summary (artifacts/api-server/src/routes/dashboard.ts) takes
  teamId, returns rank/points/budget/captain/league info, but has no competitionKey
  param and no nextKickoff field.
- GET /api/dashboard/squad, GET /api/leagues/:id/leaderboard, and
  GET /api/dashboard/top-performers are all called separately, none scoped by competition.
- TodayMatchesCard hardcodes leagueKey: "premier-league" against GET /api/fixtures.
- Global rank in the summary counts across the entire teams table, not per competition —
  this needs to become competition-scoped too, or it's misleading once both leagues show at once.
- Task #15 already gives each user one team per competition (teams.competitionKey,
  unique on userId+competitionKey) — user.teams[] should have what's needed to resolve
  both team IDs.

BACKEND TASKS:
1. Add a `competitionKey` param to GET /api/dashboard/summary. Resolve the team from
   the authenticated user's teams for that competition (server-side), not from a
   client-supplied teamId. Scope the rank calculation to only teams within that
   competition's leagues.
2. Add a `nextKickoff` field to the summary response — the earliest upcoming fixture
   time for that competition. Reuse the existing live fixture-provider logic (from the
   fixtures route/registry) rather than adding a new fixture table or sync job — factor
   out a small shared helper that returns just the next kickoff instead of the full list.
3. Scope GET /api/dashboard/top-performers and TodayMatchesCard's fixture call by
   competitionKey as well, so switching competitions on the frontend shows the right data.
4. Keep GET /api/dashboard/squad and the leaderboard endpoint as-is functionally, just
   make sure the frontend passes the correct team/league IDs for the active competition
   instead of the legacy teamId.

FRONTEND TASKS (artifacts/fanta11/src/pages/dashboard.tsx):
1. Remove the existing "Command Center" hero section entirely (the large banner with
   background image, GW points/rank/captain/budget as separate large cards).
2. Replace it with a single thin horizontal strip showing both competitions side by
   side: league name, rank (e.g. "#1 of 2"), points, and next kickoff time, each as a
   compact inline row. The active/selected competition is visually distinguished
   (e.g. subtle background tint); the other is muted. Clicking either side switches
   which competition is "active" and refetches the squad/table/top-performers below
   for that competition — use useLeagueContext() for this state, not local-only state,
   so it stays consistent with how the rest of the app tracks active competition.
3. Below the strip, keep the existing two-column layout: squad quick view on the left,
   league standings on the right — but now sourced from the active competition's team
   and league, resolved via user.teams[] / useLeagueContext() instead of
   authState.user.teamId.
4. Today's Matches and Top Performers cards below that should also reflect the active
   competition.
5. If a user has no team yet in one of the two competitions, show that competition's
   side of the strip in an empty/inactive state (e.g. "No team yet") rather than erroring.

Do not touch the Fixtures page or its provider registry — only add the reusable
next-kickoff helper there if that's the cleanest place for it. Do not add any database
migrations or new tables.

After the change, tell me exactly what you changed, which files, and how you verified
it — ideally by loading the Dashboard in the browser with a test account that has teams
in both competitions and confirming both sides of the strip show correct, different data,
and that switching between them updates the squad/table/matches/top performers correctly.